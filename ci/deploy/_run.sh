#!/bin/bash

echo "=> Deploy script by Tooit (runs on every instance). ====================="
echo
# Run something on every upgraded service instance.
# ...
echo
echo "/ End. =================================================================="
